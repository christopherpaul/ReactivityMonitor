<UserControl x:Class="ReactivityMonitor.Screens.ConnectionScreen.ConnectionScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReactivityMonitor.Screens.ConnectionScreen"
             xmlns:rm="clr-namespace:ReactivityMonitor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:ConnectionScreenViewModel}">
    <Grid>
        <ListView ItemsSource="{Binding AvailableConnections}">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type rm:Server}">
                    <TextBlock Text="{Binding Path=ProcessName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
